To do:

// foundational
Shorten shadows on mobile (or fix font size)
live update should revert text to original if input is cleared
'update blockchain' button should be disabled until wallet is connected

// dev
clean code and comments
remove references to metamask
shorten the element ids
write README